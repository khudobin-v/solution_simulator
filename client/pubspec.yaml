name: dissolution_simulator
description: "Cellular automaton dissolution simulator — Flutter client"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.4.0
  fl_chart: ^0.70.2
  google_fonts: ^6.2.1
  image: ^4.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
